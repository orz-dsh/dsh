workspace:
  shell:
    items:
      - name: powershell
        args:
          - -ExecutionPolicy
          - Bypass
          - -NoProfile
          - -File
          - "{{.target.path}}"
project:
  option:
    items:
      - name: xxxx
        value: xxxx
        match: xxxx
  script:
    imports:
      - link: "@orz-dsh/shell-lib"
