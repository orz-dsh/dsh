#!/bin/sh

lib1_test() {
  echo "lib1 test"
  echo "lib1 get option['_os']: {{ .option._os }}"
  echo "lib1 get option['_arch']: {{ .option._arch }}"
  echo "lib1 get option['_executor']: {{ .option._executor }}"
  echo "lib1 get option['_hostname']: {{ .option._hostname }}"
  echo "lib1 get option['_username']: {{ .option._username }}"
  echo "lib1 get option['test']: {{ .option.test }}"
}
